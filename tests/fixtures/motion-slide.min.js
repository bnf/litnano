import {LitElement, html, css} from 'lit';
import {classMap} from 'lit/directives/class-map.js';
import {animate} from '@lit-labs/motion';
export class MotionSlide extends LitElement {
  static properties = {
    slid: {
      type: Boolean
    }
  };
  static styles = css`.circle{background:#4682b4;position:relative;--box-size:25vw;border-radius:50%;height:var(--box-size);width:var(--box-size)}.slid{left:calc(100% - var(--box-size))}`;
  constructor() {
    super();
    this.slid = false;
  }
  render() {
    return html`<p> <button @click=${() => this.slid = !this.slid}>Slide ${'foo'}${'bar'}</button> </p><p class="${classMap({
      slid: this.slid
    })} circle" ${animate()}></p>`;
  }
}
customElements.define('motion-slide', MotionSlide);
